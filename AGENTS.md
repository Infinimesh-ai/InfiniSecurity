# AGENTS.md

## 安全纪律(强制,对所有会话生效)

本项目诞生于 2026-07-29 的 `rm -rf /` 真实事故(复盘:
`../InfiniCode/docs/LESSONS_2026-07-29_rm-rf_incident.md`)。本项目自己就是
拦截器与恢复工具——**被测对象恰恰是安全装置本身,所以测试纪律比任何项目都严**。
以下规则任何会话不得以任何理由放宽:

1. **测试样本永远不用真实弹药。** 任何可能被 exec 的字符串(测试代码、
   fixture、请求体、提示词、演示脚本)禁止使用 `rm` / `dd` / `mkfs` /
   `truncate` / `shred` / `git clean` / `find -delete` 等真实破坏性命令——
   哪怕"预期它会被拦下"。验收拦截用命中专属签名的无害命令
   (标准样本:`touch /tmp/infsec-probe-marker` + 对应规则)。
2. **审批/解锁的"批准"路径只能由人操作。** 自动化测试只允许走"驳回"路径;
   `infsec unlock` 的交互确认不得被脚本、管道或 expect 喂入,这本身是验收项。
3. **所有 e2e 跑在隔离环境。** 删除类验收只作用于 scratchpad / 临时目录里
   现造的 fixture;涉及真实执行链路的测试跑在容器或虚拟机;恢复向导(M5)
   的验收对象只能是现造的 fixture 镜像,绝不是开发机真实磁盘。
   测试进程不得拥有触碰 `~/Documents` 的权限。
4. **动手前先回答:"所有安全层都放行时,最坏会发生什么?"**
   答案不可接受就重新设计测试,与出错概率无关。
5. **每完成一个验收项就停下**,向用户说明人工复核步骤,确认后再继续。
6. **恢复功能开发中,证据只读是绝对边界。** 任何针对证据设备/镜像的写路径
   都是 bug,包括"帮忙修复文件系统"这类好心写入(禁止 `fsck -y`、
   journal replay、就地写回)。
7. **代码值得写第二天,第一天就 push 远端。** 远端:
   `https://github.com/Infinimesh-ai/InfiniSecurity.git`。

## 项目状态

M0 完成(2026-07-30 更新):计划落地,见 `docs/PLAN.md`。关键决策全部已定
(PLAN 第 7 节):二审延迟可接受、风险自适应 T0–T3 分级(备份态感知)、
拦截与恢复统一 Rust、保护目录集统一管理 + 策略自保护(2.4.2a)、
**默认 root 部署**(5.0 特权模型:root 判决核最小化、二审 LLM 绝不跑 root、
提权尝试默认 T3;非 root 降级模式递延,不列入里程碑)。
**M1 完成并验收通过(2026-07-31,31/31):** 报告见
`docs/M1-ACCEPTANCE.md`。验收机 192.168.20.17(VMware,Ubuntu 26.04,
root + test 双用户);开发机不装 root 服务。VM 实测暴露并修复两个
开发机单测覆盖不到的真实缺陷:握手竞态(普通 read 会丢 SCM_RIGHTS)、
**挂载视图分叉让路径判决静默失效**(daemon 的 PrivateTmp 导致
"截断已有文件"被判成"新建文件"放行)。
**M2 完成并验收通过(2026-07-31,15/15,M1 回归 31/31):** 报告见
`docs/M2-ACCEPTANCE.md`。风险分级(T0–T3 × S0–S4 × 情景)、备份态探测、
操作级合并判决(300 次删除 → 1 次判决 + 299 次缓存)、隔离区与
`infsec quarantine list/restore` 全部实测通过。VM 又暴露三个缺陷:
root 跑 git 探测既是提权面又让探测全失效(已改为降权到被监督用户)、
缺 CAP_SETUID/SETGID 导致降权静默失败、挂载视图判据把加固 bind mount
误判成分叉(第三次才修对,正确判据是比"底层文件是否同一个")。
**M3 完成并验收通过(2026-07-31,12/12):** 报告见 `docs/M3-ACCEPTANCE.md`。
爆发检测(速率 + 广度双维度)实测端到端 215ms 冻结整棵进程树,
用 SIGSTOP 保留现场;`infsec panic/thaw/frozen` 就位,止损清单是
事故 SOP 第 1.A 节的产品化。
**M4 完成并验收通过(2026-07-31,18/18):** 报告见 `docs/M4-ACCEPTANCE.md`。
硬链接增量快照(PLAN 开放问题 4 定稿:ext4 无原生快照,不引入外部依赖)、
`backup status` 三项缺项告警、`drill` 实际恢复 + 逐文件验哈希。
**下一步:M5 — 引导式取证恢复向导。**

## 一句话原则

凡是"执行"与"危险字符串"出现在同一条链路上,先默认它会被执行,再开始设计;
凡是碰证据的代码,先默认它会写坏证据,再开始设计。
