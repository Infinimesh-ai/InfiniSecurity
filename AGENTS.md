# AGENTS.md

## 安全纪律(强制,对所有会话生效)

本项目诞生于 2026-07-29 的 `rm -rf /` 真实事故(复盘:
`../InfiniCode/docs/LESSONS_2026-07-29_rm-rf_incident.md`)。本项目自己就是
拦截器与恢复工具——**被测对象恰恰是安全装置本身,所以测试纪律比任何项目都严**。
以下规则任何会话不得以任何理由放宽:

1. **测试样本永远不用真实弹药。** 任何可能被 exec 的字符串(测试代码、
   fixture、请求体、提示词、演示脚本)禁止使用 `rm` / `dd` / `mkfs` /
   `truncate` / `shred` / `git clean` / `find -delete` 等真实破坏性命令——
   哪怕"预期它会被拦下"。验收拦截用命中专属签名的无害命令
   (标准样本:`touch /tmp/infsec-probe-marker` + 对应规则)。
2. **审批/解锁的"批准"路径只能由人操作。** 自动化测试只允许走"驳回"路径;
   `infsec unlock` 的交互确认不得被脚本、管道或 expect 喂入,这本身是验收项。
3. **所有 e2e 跑在隔离环境。** 删除类验收只作用于 scratchpad / 临时目录里
   现造的 fixture;涉及真实执行链路的测试跑在容器或虚拟机;恢复向导(M5)
   的验收对象只能是现造的 fixture 镜像,绝不是开发机真实磁盘。
   测试进程不得拥有触碰 `~/Documents` 的权限。
4. **动手前先回答:"所有安全层都放行时,最坏会发生什么?"**
   答案不可接受就重新设计测试,与出错概率无关。
5. **每完成一个验收项就停下**,向用户说明人工复核步骤,确认后再继续。
6. **恢复功能开发中,证据只读是绝对边界。** 任何针对证据设备/镜像的写路径
   都是 bug,包括"帮忙修复文件系统"这类好心写入(禁止 `fsck -y`、
   journal replay、就地写回)。
7. **代码值得写第二天,第一天就 push 远端。** 远端:
   `https://github.com/Infinimesh-ai/InfiniSecurity.git`。

## 项目状态

M0 完成(2026-07-29):计划落地,见 `docs/PLAN.md`。下一步是 M1
(seccomp 监督器 MVP),动手前先解决 PLAN 第 6 节的开放问题(至少定语言选型)。

## 一句话原则

凡是"执行"与"危险字符串"出现在同一条链路上,先默认它会被执行,再开始设计;
凡是碰证据的代码,先默认它会写坏证据,再开始设计。
