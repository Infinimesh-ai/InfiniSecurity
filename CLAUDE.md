# CLAUDE.md

**动手前先完整读 `AGENTS.md`——那里的七条安全纪律对本会话强制生效,
任何理由不得放宽。** 本项目是拦截器与恢复工具本身,被测对象就是安全装置,
测试纪律比任何项目都严。最不可妥协的三条:

1. 测试样本永不用真实破坏性命令(标准无害样本:`touch /tmp/infsec-probe-marker` + 专属签名);
2. 删除类验收只作用于临时目录里现造的 fixture,测试进程不得有触碰 `~/Documents` 的权限;
3. 动手前先回答"所有安全层都放行时最坏会发生什么",答案不可接受就重新设计。

计划与当前状态:`docs/PLAN.md`(M0 已完成,决策见第 7 节,下一步 M1,
无阻塞项可直接开工)。项目起源:2026-07-29 `rm -rf /` 真实事故,
复盘在 `../InfiniCode/docs/LESSONS_2026-07-29_rm-rf_incident.md`。
